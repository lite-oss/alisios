[build]
  command   = "npm run build"
  publish   = "_site"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-sitemap"

# Netlify Identity — requerido para Decap CMS
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "SAMEORIGIN"
