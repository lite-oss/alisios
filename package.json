{
  "name": "pass-a",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tinacms dev -c \"astro dev\"",
    "build": "tinacms build && astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^4.16.0",
    "tinacms": "^2.6.0",
    "@tinacms/cli": "^1.6.0"
  }
}
